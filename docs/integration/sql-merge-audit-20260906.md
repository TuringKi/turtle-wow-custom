# 两次上游合并 SQL 执行审计（2026-09-06）

比对原定制版本 c71155a 与合并后 1629c24；按 Git 新增/修改 SQL 文件清点，包括模块目录。完整文件清单见 [sql-audit-inventory.tsv](sql-audit-inventory.tsv)。这是本次会话的执行证据清单，不宣称文件在服务器全部历史中从未执行，也不以迁移表没有哈希为未执行证明。被删除的历史 SQL 文件不作为重新运行的候选。

- 11 个定制迁移/预检文件：00 只读，01–04、10–15 已执行。随后执行的 16 为孤立记录保护版，见上游同步报告。
- 3 个上游文件已逐项确认完整/部分结果：幻化菜单、法术脚本绑定、孤立路径清理。
- 155 个世界内容候选原文件没有整文件导入；部分建表、系数复制及机器人数据由定制迁移代替。不能把“未导入原文件”等同于其中每一行都缺失。
- 226 个其他新增/修改 SQL 未在本次会话直接执行，包含历史补丁、基础脚本、账号/角色及模块脚本；需按内容逐项判定，禁止批量运行。

## 后续补齐执行结果

用户要求审核并补齐后，已执行定制增量 **17–23**。完整范围、保留项及验证限制见 [merge-completion-20260906.md](merge-completion-20260906.md)。法术绑定重新按全部候选补丁执行后的最终状态核对，不能使用单个早期 SQL 的赋值代替最终结果。

- 889 条既有法术空绑定补齐，新增 3,050 条完整法术及对应 spell_extra；1,054 条可实现的最终绑定逐项匹配。
- 6 个 NPC/物件/副本脚本空绑定、1,114 条功能种子和 353 条法术辅助记录补齐。
- 修正 4 条战场记录中 8 个与安装版 DBC 不匹配的位置引用；保留其他自定义字段。
- 角色库只扩大机器人策略字段容量至 varchar(4000)，没有账号/角色行删除或覆盖。
- 140 份路线/回退文件及 roster 已部署到 `.install/share/mod-dungeon-clear`，主配置已指向该目录。

最终逐项绑定结果见 [sql-audit-final-spell-bindings.tsv](sql-audit-final-spell-bindings.tsv)。下方 712 项表格及旧 TSV 是补齐前的早期补丁快照，不代表当前缺口。

## 首轮审计的功能缺口（补齐前历史快照）

只读逐项核对 `20260721013813_world.sql` 中最终对应 712 个法术编号的 script_name 赋值，详见 [sql-audit-spell-bindings.tsv](sql-audit-spell-bindings.tsv)：

| 状态 | 编号数 |
| --- | ---: |
| 当前绑定符合上游（邮箱修复） | 2 |
| 法术存在但 script_name 为空 | 670 |
| 法术记录缺失 | 40 |
| 非空绑定与上游不同 | 0 |

这说明此前完成的是启动结构补齐，不是全部功能迁移。涉及职业及物品脚本，邮箱只是其中一例。在当时的只读审计阶段，670 条绑定及 40 条缺法术尚未写入：补绑定会启用相应功能，需要形成明确执行范围；缺法术必须另核对数值及客户端兼容，不能仅为绑定插入空法术记录。此审计尚不能证明与用户报告的客户端出本空指针崩溃有关。

已有全量世界内容对比见 world-data-review.md，32 张表存在定制差异；本轮没有逐行重放这些大规模内容覆盖，也没有重新运行账号/角色初始化或删除脚本。
