# 任务：复审两次 Tortoise 合并及其运行依赖

范围：首次合并 `b473640`（本地 `c71155a`、上游 `6cdc923`），增量合并 `1629c24`（上游 `c7d4627`），以及随后安装和补丁。逐项追溯整合边界，不以编译、启动成功代替游戏内验证。账号、角色数据库不执行删除或整库覆盖；本次生产数据库仅只读查询。

## 任务清单

- [x] 建立两次合并文件清单，复核原 46 个冲突文件及核心/模块接口差异。
- [x] 核对旧定制保留、两套机器人构建隔离和战场路径符号隔离。
- [x] 审查会话识别、登录、组队命令、传送确认、地图移除和退出接口。
- [x] 审查机器人账号创建和启动删除路径，核对当前前缀与删除配置。
- [x] 复核已执行 SQL 的关键结果、配置路径及安装资源。
- [x] 修复版完整编译、回归测试和隔离机器人传送验证；关服异常单独保留。
- [x] 安装修复版，记录校验值；源码和本记录随本次修复提交推送。
- [ ] 真实 1.18.1 客户端测试：组队走入副本、召唤、出本、重复进出。

## 本轮发现

| 编号 | 证据及影响 | 归因 | 处理 |
| --- | --- | --- | --- |
| MR-01 | 上游无 socket 会话写入 `<BOT>`，首次合并保留本地 `<PBOT>`，但 `PlayerbotAI::IsRealPlayer` 仍只接受 `<BOT>`/`disconnected/bot`。机器人因此跳过 far-teleport ACK，并被 summon 拒绝。 | 明确的合并兼容遗漏 | 统一 `BotSession.h` 判断；AI 两个重载、登录控制器、挑战设置、账号在线标记及移动诊断共用。保留真人和机器人控制权限检查。 |
| MR-02 | `Map::Remove(Player*)` 保留旧 ike3 的“不清空 AI 角色 map 指针”规则；现代 AI getter 接入模块槽后也命中。与此同时 FindPlayer 默认返回不在世界内的角色；这两处语义需要联合验证。 | 继承的本地行为与上游不同，风险项 | 本轮未直接改全局地图/查找语义；单独恢复 ResetMap 会让现有调用拿到 map 为空的角色。列入后续逐调用点验证，尚不能判定为本次故障根因。 |
| MR-03 | `CreateRandomBots` 在 DeleteRandomBotAccounts=0 时仍执行临时角色/空账号清理，数据库 bot_delete 事件也可绕过开关。 | 引入的上游行为未满足本服保护约束 | 自动清理和排队删除统一受显式删除开关控制。当前开关为 0；未在生产执行清理。此保护不等于禁止游戏正常存档或所有管理员删除命令。 |
| MR-04 | 上次召唤测试把 IsRealPlayer 模拟为布尔值，没有覆盖核心实际会话标记。 | 我方验证漏项 | 测试提取实际核心标记和实际 AI 判定函数；旧版代码会失败，修复后通过。 |
| MR-05 | 隔离 MySQL 8 实测组队触发 1064：Group.cpp 的 4 条 INSERT/UPDATE 未引用 groups 表名，导致测试服数据库线程断言退出。 | 合并前本地与上游均有，非本次 merge 新增；生产只读确认版本为 5.6.34 | 为 4 条语句加反引号；不变更表结构或数据。通过实际组队重新验证。 |

入口 0.5/5 码、移动距离二次开方等问题来自上游模块，本地此前 `43b7b32` 已修复；它们不能代替 MR-01。此前 SQL 功能依赖、模块配置和运输路径未随部署一起对齐，是已经补齐的整合遗漏。909702 的随机次要目标缺失来自上游脚本原有行为；客户端 ERROR #132 仍未通过真实客户端复现或证实完全解决，不能把所有问题归因于同一处 merge。

## 审查证据

- 首次合并对第一父提交改动 6,508 个文件；增量合并 74 个文件，仅路线、roster、SQL 和记录文档，没有编译源码改动。
- 对比上游与当前代码，差异包含大量保留的本地实现。595 个旧机器人文件中 592 个逐字节相同，3 个为原记录的兼容修改；native partybot/battlebot 文件未丢失。现代战场路径 cpp/h 反向还原符号重命名并忽略缩进后与上游相同。
- 核对 `GetBot`（native）、模块 AI 槽（modern）及旧字段（legacy）的不同用途；native 临时角色的存档例外没有套到现代持久角色。旧更新入口由 BUILD_LEGACY_PLAYERBOTS 隔离，现代模块使用注册钩子。
- 显式新增表、角色策略字段、最终法术绑定、12 条船/飞艇路径及 909702 标志按生产库现状只读复核。既有排除项：8 个缺少实现的 Wrath 绑定及错误的 27230 治疗石绑定。
- 再次对 7z 提取参考与安装目录逐文件 SHA-256 比对：158 DBC、2,805 maps、6,923 vmaps、2,168 mmaps 均无缺失或差异；44 个核心 DBC 的字段数与记录大小正常。安装目录另有 176 个旧额外文件，保留未删。140 个路线/回退文件和 roster 与源码一致。
- 编译模式为 modern ON、legacy OFF、Eluna ON。安装脚本使用 `.install` 下的显式配置和资源路径。构建缓存的默认安装前缀仍为 `/root/server`，因此不能直接运行不带路径覆盖的 `cmake --install`；当前使用已核验的 `.install` 包装入口和原子安装。
- 账号创建先检查同名账号存在，已有账号不会通过 CreateAccount 改密码；本服前缀为 NB1181。但“已有同名前缀账号被当作机器人池使用”不同于“覆盖账号”，前缀不能作为任意数据库中绝对无冲突的证明。

本机详细清单、差异和只读结果：`/root/turtle-wow-startup-check/merge-review-20260906/`；本轮编译、测试和安装记录：`/root/turtle-wow-startup-check/bot-session-marker/`。未将数据库导出、程序或资源包提交 Git。

## 本轮验证与安装

- Release 全量编译及最终增量编译通过；构建源与审阅源一致。
- 回归测试通过：核心实际会话标记的 5 种身份 + 13 种召唤状态/限制；关闭删除开关时有无排队删除事件均无自动清理写入。两项测试使用修复前代码均能检出失败。
- 隔离 MySQL 8 与合成账号实际创建两人队伍：`rndbot group size=2 name=Auditlast`，队长 Checkbb、队友 Auditlast。`rndbot summon *` 四次成功，实时位置为死亡矿井 map 36 → 西部荒野 map 0 → map 36；两名角色的地图移除日志均确认 instance 101。
- 这是无客户端的真实核心/机器人会话、组队及传送测试；不代替真人走副本入口、导航和客户端出本验证。
- 最后一次隔离运行无选定 SQL/断言/Eluna 错误，但关闭数据库连接后退出码 -11，**关服验证未通过**。第一轮无组队隔离运行退出码 0；第二轮检出旧 SQL 引用错误，修复后组队及传送执行成功。
- 程序已原子安装到 `/root/turtle-wow-custom/.install/libexec/mangosd`，SHA-256：`414e60073d5f008fce7733837596ed2b3833e6b820d9c3248774ebb52343ab3f`。原程序备份 `/root/turtle-wow-startup-check/bot-session-marker/mangosd-before`。未启动、停止或重启生产世界服；启动/重启后才会加载替换程序。
- 本轮未修改生产数据库；无需额外 SQL 迁移或配置变更。隔离数据库中的合成角色、账号和传送点仅供测试。

## 尚需验证的边界

- 实际玩家客户端的副本进出、客户端崩溃复测、不同副本准入条件与实际导航，仍需运行验证。
- 隔离关服 SIGSEGV 本轮再次出现于关闭数据库连接之后，尚无有效崩溃栈；需要继续定位。
- 核心 MovementHandler/Player 的诊断宏受 BUILD_PLAYERBOTS 预处理开关控制，但本轮 Ninja 编译参数没有该宏，因此不能把没有 worldport-ack 日志解释为 ACK 未执行。这一诊断构建缺口待补；本轮通过模块召唤日志、实时位置及同一 Map 指针检查验证传送。
- ObjectAccessor::FindPlayer 的默认 inWorld 参数沿用本地 false，与上游 true 有差异；不能为了这次召唤直接全局改默认值，需要逐调用点验证。召唤恢复路径已显式使用 unfiltered lookup。
- 32 张世界内容表的定制差异、地图 818 等缺失完整内容，仍按原审查保留，不能把上游数据整表覆盖。
- 检查覆盖合并清单、冲突解决和主要运行边界，不代表已经逐条实测所有导入的副本、职业、战场和历史脚本。

## 补充：PBOT / BOT 全部替换核对

结论：不应全局将 pbot 替换为 bot。按当前受版本控制的核心、模块及脚本检索，并只读检查未跟踪的旧模块副本：

| 用途 | 位置 | 处理 |
| --- | --- | --- |
| 无 socket 会话的实际标记 `<PBOT>` | WorldSession.cpp:109 | 保留；统一身份判断兼容它。改字面量不能替代修复所有消费者。 |
| 旧 ike3 登录/创建传入 `<PBOT>` | CharacterHandler.cpp 的 BUILD_LEGACY_PLAYERBOTS 分支、src/modules/Bots/playerbot/RandomPlayerbotFactory.cpp | 当前 legacy OFF，不参与此登录路径；无需改名。 |
| native 机器人传入 `<BOT>`；现代模块各创建入口传入 `disconnected/bot` | PlayerBots/PlayerBotMgr.cpp、现代 PlayerbotMgr / RandomPlayerbotFactory / PlayerbotHelpMgr / DebugAction | 均为无 socket 构造，实际由核心写入 `<PBOT>`；统一判断已兼容三种标记。修正后两处误导性注释。 |
| 判断三种标记的字符串比较 | src/shared/BotSession.h | 当前受版本控制的核心/模块中，直接比较这些标记的代码仅在此处；AI 两个重载及登录、移动判断均调用统一函数。 |
| `.pbot` 命令 | Chat.cpp:1046 | 与 `.bot` 指向同一个 HandlePlayerbotCommand；保留兼容别名，替换会形成重复命令名。 |
| `pBot`、`StopBot`、`GroupBots` 等标识符以及帮助文本的 `<bot>` | native AI、模块和命令帮助 | 变量、函数或占位符，与会话身份标记无关。 |
| 未跟踪旧副本的 IsRealPlayer 只识别 `disconnected/bot` | src/modules/playerbots/playerbot/PlayerbotAI.h:591–592 | 当前安装构建的 Ninja 清单没有该目录；不改用户的独立副本。若未来接入，需要单独修复，单换成 `<BOT>` 仍不兼容当前核心。 |

实际安装构建在 `/root/turtle-wow-merge-build`，modern ON、legacy OFF；用户当前 `.build` 是另一份旧构建缓存，不能据其目录名判断已安装版本。本次补充只改注释和审阅记录，程序行为与已验证的 `1de0ca1` 相同，无需替换数据库、账号前缀或资源。
